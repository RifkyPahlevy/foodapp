class SizeItem {
  int id;
  double sizeWidth;
  String name;
  bool isChoice;
  SizeItem(
      {required this.id,
      required this.sizeWidth,
      required this.name,
      required this.isChoice});
}
